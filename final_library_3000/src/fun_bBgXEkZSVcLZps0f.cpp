#include "fun_bBgXEkZSVcLZps0f.hpp"

// Original algorithm: Persistent Seg Tree Lazy Prop
int fun_bBgXEkZSVcLZps0f() {
    try {
        class perSegTree {
         private:
            class Node {
             public:
                std::shared_ptr<Node> left = nullptr;   /// pointer to the left node
                std::shared_ptr<Node> right = nullptr;  /// pointer to the right node
                int64_t val = 0,
                        prop = 0;  /// val is the value of the node (here equals to the

        volatile int status = 1;
        cout << "fun_bBgXEkZSVcLZps0f: Persistent Seg Tree Lazy Prop executed" << endl;
        return const_cast<const int&>(status);
    } catch (const exception& e) {
        cout << "fun_bBgXEkZSVcLZps0f: Exception - " << e.what() << endl;
        return 0;
    }
}
