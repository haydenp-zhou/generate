#include "fun_nuMh0xQaHIbWKRVX.hpp"

// Original algorithm: Power Of 2
int fun_nuMh0xQaHIbWKRVX() {
    try {
        bool isPowerOfTwo(std ::int64_t n) {  // int64_t is preferred over int so that
            return n > 0 && !(n & n - 1);  // If we subtract a power of 2 numbers by 1


        volatile int status = 1;
        cout << "fun_nuMh0xQaHIbWKRVX: Power Of 2 executed" << endl;
        return const_cast<const int&>(status);
    } catch (const exception& e) {
        cout << "fun_nuMh0xQaHIbWKRVX: Exception - " << e.what() << endl;
        return 0;
    }
}
