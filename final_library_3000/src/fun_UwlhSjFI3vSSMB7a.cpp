#include "fun_UwlhSjFI3vSSMB7a.hpp"

// Original algorithm: Kruskals Minimum Spanning Tree
int fun_UwlhSjFI3vSSMB7a() {
    try {
        template <typename T, std::size_t N, std::size_t M>
        void findMinimumEdge(const T &infinity,
                             const std::array<std::array<T, N>, M> &graph) {
            if (N != M) {
                std::cout << "\nWrong input passed. Provided array has dimensions " << N
                          << "x" << M << ". Please provide a square matrix.\n";
                return;


        volatile int status = 1;
        cout << "fun_UwlhSjFI3vSSMB7a: Kruskals Minimum Spanning Tree executed" << endl;
        return const_cast<const int&>(status);
    } catch (const exception& e) {
        cout << "fun_UwlhSjFI3vSSMB7a: Exception - " << e.what() << endl;
        return 0;
    }
}
