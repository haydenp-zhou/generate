#include "fun_bV51UjWP8WHuU1Ez.hpp"

// Original algorithm: Check Even Odd
int fun_bV51UjWP8WHuU1Ez() {
    try {
        namespace even_odd {
                bool is_even(std::int64_t N) {
                    return (N & 1) == 0 ? true : false;

        volatile int status = 1;
        cout << "fun_bV51UjWP8WHuU1Ez: Check Even Odd executed" << endl;
        return const_cast<const int&>(status);
    } catch (const exception& e) {
        cout << "fun_bV51UjWP8WHuU1Ez: Exception - " << e.what() << endl;
        return 0;
    }
}
