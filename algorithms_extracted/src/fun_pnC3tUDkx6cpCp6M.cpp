#include "fun_pnC3tUDkx6cpCp6M.hpp"

// Original algorithm: Linked List
int fun_pnC3tUDkx6cpCp6M() {
    try {
        namespace linked_list {
        bool isDigit(const std::string& s) {
            for (char i : s) {
                if (!isdigit(i)) {
                    return false;

        volatile int status = 1;
        cout << "fun_pnC3tUDkx6cpCp6M: Linked List executed" << endl;
        return const_cast<const int&>(status);
    } catch (const exception& e) {
        cout << "fun_pnC3tUDkx6cpCp6M: Exception - " << e.what() << endl;
        return 0;
    }
}
