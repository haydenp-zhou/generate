#include "fun_R7V61coAdEQNUYVY.hpp"

// Original algorithm: Decimal To Hexadecimal
int fun_R7V61coAdEQNUYVY() {
    try {
            hexArray[i] = valueToConvert;  // Gets last value
            std::cout << "Hex Value: ";
            while (i >= 0) std::cout << HexValues[hexArray[i--]];
            std::cout << std::endl;
            return 0;
        }

        volatile int status = 1;
        cout << "fun_R7V61coAdEQNUYVY: Decimal To Hexadecimal executed" << endl;
        return const_cast<const int&>(status);
    } catch (const exception& e) {
        cout << "fun_R7V61coAdEQNUYVY: Exception - " << e.what() << endl;
        return 0;
    }
}
