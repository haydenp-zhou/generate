#include "fun_duovKesrFy8nFxtQ.hpp"

// Original algorithm: N Choose R
int fun_duovKesrFy8nFxtQ() {
    try {
        template <class T>
        T n_choose_r(T n, T r) {
            if (r > n / 2) {
                r = n - r;  // Because of the fact that  nCr(n, r) == nCr(n, n - r)

        volatile int status = 1;
        cout << "fun_duovKesrFy8nFxtQ: N Choose R executed" << endl;
        return const_cast<const int&>(status);
    } catch (const exception& e) {
        cout << "fun_duovKesrFy8nFxtQ: Exception - " << e.what() << endl;
        return 0;
    }
}
