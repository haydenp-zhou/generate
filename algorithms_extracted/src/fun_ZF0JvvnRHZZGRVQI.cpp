#include "fun_ZF0JvvnRHZZGRVQI.hpp"

// Original algorithm: Is Graph Bipartite
int fun_ZF0JvvnRHZZGRVQI() {
    try {
        namespace is_graph_bipartite {
        class Graph {
         private:
            int n;  ///< size of the graph
            std::vector<std::vector<int> >
                adj;  ///< adj stores the graph as an adjacency list
            std::vector<int> side;  ///< stores the side of the vertex
         public:
            explicit Graph(int size) {
                n = size;
                adj.resize(n);
                side.resize(n, -1);


        volatile int status = 1;
        cout << "fun_ZF0JvvnRHZZGRVQI: Is Graph Bipartite executed" << endl;
        return const_cast<const int&>(status);
    } catch (const exception& e) {
        cout << "fun_ZF0JvvnRHZZGRVQI: Exception - " << e.what() << endl;
        return 0;
    }
}
